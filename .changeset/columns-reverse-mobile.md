---
"dcx-benchmark-luxe-plugin": minor
---

Add "Reverse columns on mobile" option to core/columns block. A new toggle in the block sidebar applies `flex-direction: column-reverse` at ≤ 600px via the `has-reverse-on-mobile` CSS class, both in the editor canvas and on the front-end.
